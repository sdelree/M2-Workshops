
<!DOCTYPE html>
<html lang="en">
<head>
    <% include ./head %>
</head>
<body class="container">
<% if (locals.workshop) { %>
<form action="/update-workshop/<%= workshop.name %>" method="POST">
<% } else { %>
<form action="/workshop" method="POST">
<% } %>
    <div class="form-group">
        <label for="name">Name</label>
        <input type="text" class="form-control" id="name" <% if (locals.workshop) {%> value="<%= workshop.name %>" <% }%>
         placeholder="Workshop Name" name="name" required>
    </div>
    <div class="form-group">
        <label for="description">Description</label>
        <input type="text" class="form-control" id="description" <% if (locals.workshop) {%> value="<%= workshop.description %>"<%} %>
        placeholder="Give a description for your workshop" name="description" required>
    </div>
    <button type="submit" class="btn btn-primary">Save</button>
    <a type="button" class="btn btn-secondary" href="/">Cancel</a>
</form>

<footer>
    <% include ./footer %>
</footer>

</body>
</html>
